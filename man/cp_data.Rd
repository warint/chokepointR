% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cp_data.R
\name{cp_data}
\alias{cp_data}
\title{Query chokepoint risk data}
\usage{
cp_data(locations = NULL, risks = NULL, levels = NULL, category = NULL)
}
\arguments{
\item{locations}{Character vector of chokepoint location(s).}

\item{risks}{Character vector of risk code(s) (see \code{\link{cp_risk}}).}

\item{levels}{Character vector of risk level(s): "Low risk", "Medium risk",
"High risk".}

\item{category}{Character vector of risk category/categories.}
}
\value{
A data frame of matching rows from \code{\link{chokepoint_risks}}.
}
\description{
Find and return chokepoint risk records matching the selected parameters. Any
argument left as \code{NULL} (the default) is not filtered on, so
\code{cp_data()} returns the complete dataset.
}
\examples{
myData <- cp_data(locations = "Panama Canal", risks = "S-T")
myData <- cp_data(locations = c("Panama Canal", "Suez Canal"),
                    risks = c("S-T", "S-C"))
myData <- cp_data(category = "Weather and climate risk")
myData <- cp_data(levels = "High risk")
myData <- cp_data("Panama Canal", "S-T")
myData <- cp_data()
}
\seealso{
\code{\link{cp_location}} for the locations list,
  \code{\link{cp_risk}} for risk codes, and \code{\link{chokepoint_risks}} for the
  dataset itself.
}
